---
type: engagement
id: audit
surface: audit
window_days: 30
status: tracking
evidence-posthog:
  fetched_at: null
  source: "not-yet-synced"
  window_days: 30
  unique_visitors: 0
  pageviews: 0
  audit_requests: 0
  request_conversion: 0
last-synced: null
---

# Audit page engagement

Ongoing engagement snapshot for the Readiness Audit request page (`/audit`), synced
from PostHog. Not a hypothesis — a standing record of the paid-audit funnel: unique
visitors, the `section_viewed` funnel (`audit-hero` → `audit-form`), and the
`audit_requested` conversion event (the form submit, broken down by the `qualifier`).

`systemix evidence engagement pull` writes the latest numbers into the
`evidence-posthog` block above and appends a dated entry below. A human reviews the
queued card and acknowledges it (or flags it for an experiment).

Rolls up into the **consultancy-leads** goal (`experiments/goals/consultancy-leads.mdx`):
`/audit` is a funnel step toward a consultancy conversation — the offer ladder is
free audit → **paid audit** → kit → sprint.

## Engagement Log


### not yet synced

No evidence pulled yet. Run `systemix evidence engagement pull` once `/audit` has traffic.
